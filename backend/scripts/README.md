# Backend Scripts

This directory contains one-off operational scripts for the Plum backend.

## Available Scripts

| Script | Purpose |
|--------|---------|
| *(none yet)* | Future: seed scripts, migration scripts, data export utilities |

## Usage

All scripts should be run from the `backend/` directory with the virtual environment activated:

```bash
cd backend
source venv/bin/activate   # Windows: venv\Scripts\activate
python scripts/<script_name>.py
```
